# forklift

Listener for Docker Hub webhooks.

## Installation
`pip install -r requirements.txt`
`python forklift.py`

## Development
`pip install -r requirements_dev.txt`

### Running Tests
`pytest`
